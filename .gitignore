# Secrets / credentials — NEVER commit these
.env
secrets.env
*.env
!*.env.example

# Config backups & baselines — live in a separate, private repo, never here
backups/
configs/
baselines/
*.xml
*.json.cfg

# Real device/Panorama inventory (real IPs/hostnames/API keys) — only the
# sanitized example is public
config/config.yaml

# Tool output
reports/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Sanitized sample configs used as test fixtures — SAFE to commit (fake data only)
!tests/fixtures/
!tests/fixtures/*.xml

# Claude
CLAUDE.md
CLAUDE.local.md
.claude/settings.local.json
